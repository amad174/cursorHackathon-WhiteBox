{
  "name": "todo-app-example",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --reporter=default --reporter=json --outputFile=.vitest-result.json",
    "test:coverage": "vitest run --coverage --reporter=default --reporter=json --outputFile=.vitest-result.json"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.1.1",
    "typescript": "^5.8.2",
    "vitest": "^3.1.1"
  }
}
