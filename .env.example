# Copy to .env (not committed) or export in your shell.
# Used only with: node dist/cli.js review --root <dir> --ai
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=https://api.openai.com/v1
